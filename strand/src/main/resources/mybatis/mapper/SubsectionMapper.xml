<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.strand.mapper.SubsectionMapper">
    <select id="getSubsectionSummariesForSection" resultType="SubsectionSummary">
        SELECT
            s.id_subsection as idSubsection,
            s.title
        FROM subsection as s
        WHERE s.id_section = #{idSection}
    </select>

    <insert id="insertSubsection" parameterType="InsertSubsectionRequest" useGeneratedKeys="true"
    >
        INSERT INTO subsection(title, description, id_section)
        VALUES(#{title}, #{description}, #{idSection})
    </insert>

    <update id="updateSubsection" parameterType="UpdateSectionRequest" >
        UPDATE subsection
        SET title       = #{title},
            description = #{description}
        WHERE id_section = #{idSection}
    </update>

    <update id="hideSubsection">
        UPDATE subsection
        SET hidden = #{isHidden}
        WHERE id_subsection = #{idSubsection}
    </update>

    <update id="lockSubsection">
        UPDATE subsection
        SET locked = #{isLocked}
        WHERE id_subsection = #{idSubsection}
    </update>

    <delete id="deleteSubsection" parameterType="java.lang.Integer" >
        DELETE
        FROM subsection
        WHERE id_subsection = #{idSubsection}
    </delete>
</mapper>