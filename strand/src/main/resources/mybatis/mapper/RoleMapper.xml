<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="me.strand.mapper.RoleMapper">
    <select id="getRolesByGroupId">
        SELECT
            r.id_role,
            r.name
        FROM role AS r
        JOIN role_group AS rg ON r.id_role = rg.id_role
        WHERE rg.id_user_group = #{groupId}
    </select>
</mapper>